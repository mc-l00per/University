A.
  int ValorAbsoluto (int entero){
    if (entero < 0)
      entero = -entero;
      //faltaria el return
      return entero;
    else
      return entero;
  }
B.
  //cambio char por int que es lo que devuelve el return
  int Cuadrado (int entero){
    return entero * entero;
  }
C.
  void Imprime(double valor){
    //no necesita declarar el double 
    //double valor
    
    cout << valor;
  }
D.
  bool EsPositivo(int valor){
    if (valor > 0)
      return true;
    //faltaria un else para que devuelva false
    else
      return false;
  }
