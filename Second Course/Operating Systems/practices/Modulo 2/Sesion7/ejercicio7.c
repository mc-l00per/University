manu=open (txt)
    
dup2(manu, STDERR_FILENO)
    
    if(comprobacion)
        perror(txt,"error en lectura")


while(readdir)
    contador++;



int fd[contador][2];

for(contador)
    pid[i]=fork()

    if(pid[i]==0)
        cerrar fd[i][0];
        dup2(fd[1],STDOUT_FILENO)
        exclp(esclavo)
    else
        waitpid(pid[i])
        cerrar fd[i][1]
        while(read)
            suma+=read(...)

